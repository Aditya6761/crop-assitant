Drop your evidence screenshots here before submitting:

- `test-run.png` — terminal output of `npm test` passing
- `coverage-summary.png` — coverage % from `npm run test:coverage`
- `lighthouse-mobile.png` / `lighthouse-desktop.png`
- `axe-or-wave.png` — accessibility scan with zero AA violations
